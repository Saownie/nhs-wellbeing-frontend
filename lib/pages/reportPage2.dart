import 'package:flutter/material.dart';

class ReportPage2 extends StatelessWidget {
  const ReportPage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Harrassment Reporting"),
      
      ),
    );
  }
}